mutation CreateChannel($input: ChannelInput!) {
  createChannel(input: $input) {
    id
    name
  }
}

mutation EnsureChannel($input: ChannelInput!) {
  ensureChannel(input: $input) {
    id
    name
  }
}
